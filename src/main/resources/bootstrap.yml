spring:
  cloud:
    nacos:
      discovery:
        server-addr: 172.18.1.84:8848
      config:
        server-addr: 172.18.1.84:8848
        name: servers-one
        encode: utf-8
        group: DEFAULT_GROUP
        file-extension: yml
  application:
    name: servers-one
